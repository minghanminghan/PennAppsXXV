import { OrgMemberInfo, User } from "@propelauth/javascript";
import React from "react";
export declare type UserInformationForTesting = {
    user: User;
    orgMemberInfos: OrgMemberInfo[];
    accessToken?: string;
};
export declare type AuthProviderForTestingProps = {
    loading?: boolean;
    userInformation?: UserInformationForTesting;
    activeOrgFn?: () => string | null;
    children?: React.ReactNode;
};
export declare const AuthProviderForTesting: ({ loading, userInformation, activeOrgFn, children, }: AuthProviderForTestingProps) => JSX.Element;
